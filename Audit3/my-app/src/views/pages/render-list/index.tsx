import UIList from "./UiList";

const RenderList = () => {
  const arrColor = ["red", "blue", "yellow", "brown"];

  return (
    <>
      <UIList color={arrColor} />
    </>
  );
};

export default RenderList;
